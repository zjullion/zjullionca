{
  "name": "send-email",
  "private": true,
  "engines": {
    "node": "20",
    "pnpm": "8"
  },
  "scripts": {
    "build": "esbuild src/index.ts --bundle --outfile=dist/index.js --external:/opt/nodejs/* --platform=node --target=node20",
    "compile": "tsc",
    "test": "jest"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.133",
    "@types/jest": "^29.5.12",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.2",
    "ts-node": "^10.9.2",
    "typescript": "^4.8.4"
  }
}
